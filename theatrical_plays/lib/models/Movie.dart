class Movie {
  int id;
  String title;
  String ticketUrl;
  String producer;
  String mediaUrl;
  String duration;
  String description;

  Movie(this.id, this.title, this.ticketUrl, this.producer, this.mediaUrl,
      this.duration, this.description);
}
